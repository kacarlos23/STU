# Local basemap archives

Place approved `.mbtiles` or `.pmtiles` basemap archives in this directory.

Only OpenStreetMap-derived basemap data belongs here. Private STU property and visit layers must be served by the authenticated API.
