name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      STU_POSTGRES_PASSWORD: ci-compose-validation-only
      STU_DB_CONNECTION: Host=database;Port=5432;Database=stu;Username=stu;Password=ci-compose-validation-only
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: dotnet restore STU.slnx
      - run: dotnet build STU.slnx --configuration Release --no-restore
      - run: dotnet test STU.slnx --configuration Release --no-build
      - run: npm ci
      - run: npm run lint:web
      - run: npm run test:web
      - run: npm run build:web
      - run: docker compose config --quiet
